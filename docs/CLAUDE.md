# Project Documentation

This directory contains project documentation. Each subdirectory has a
`CLAUDE.md` for progressive discovery — start there when entering a topic.

## Installed by MLE

MLE provides rules, skills, and agents that are loaded automatically:

- `.claude/rules/` — code standards, testing, security, architecture, git policy
  (loaded by path scope when editing matching files)
- `.claude/skills/` — MLE workflow skills for SDLC phases
  (triggered by intent patterns in your prompts)
- `.claude/agents/` — specialist agents for review, analysis, infrastructure

## Conventions

- Each `docs/` subdirectory should contain a `CLAUDE.md` explaining what's available
- Cross-link to source code using repository-root paths
- British English for user-facing text

---

_Generated by `mle init` — edit freely._
